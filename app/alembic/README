Generic single-database configuration.

Use python version 3.12.0 or above 3.10 And activate virtual environment
install dependencies through pip install -r requirement.txt
create database and give the database name and password in main sqlalchemy_url 
migration
   alembic init alembic
   alembic revision --autogenerate -m "initial"
   alembic upgrade head
run the application using uvirocn main:app --reload






